N->L->M->E->A(get surf)->G->F->B->C->Q(get fly? too early?)->H->P->I->J->K->D(endgame) ->Q(postgame)
 
 might be doable, but that's an early fly and quite a lot of backtracking over early routes, so route 1/2 will need some kind of shortcut
